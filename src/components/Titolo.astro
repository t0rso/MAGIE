---
/** Intestazione di sezione: occhiello + titolo con sottolineatura a pennello. */
interface Props { occhiello?: string; brush?: string; class?: string; center?: boolean; tag?: 'h1' | 'h2' }
const { occhiello, brush = 'var(--color-limone)', class: cls = '', center = false, tag = 'h2' } = Astro.props;
const Tag = tag;
---
<div class:list={['reveal', center && 'text-center', cls]}>
  {occhiello && <p class="text-[11px] sm:text-xs font-black uppercase tracking-[.2em] text-fragola mb-3">{occhiello}</p>}
  <Tag class:list={['font-semibold', tag === 'h1' ? 'text-[2.4rem] leading-[1.05] sm:text-5xl md:text-6xl' : 'text-3xl sm:text-4xl md:text-5xl']} style={`--brush:${brush}`}>
    <slot />
  </Tag>
  {Astro.slots.has('sotto') && <p class="mt-4 text-base sm:text-lg text-cacao/85 max-w-2xl" class:list={[center && 'mx-auto']}><slot name="sotto" /></p>}
</div>
